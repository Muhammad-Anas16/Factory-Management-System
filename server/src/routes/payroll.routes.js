import{Router}from'express';import{list,create,update,remove}from'../controllers/payroll.controller.js';import{requireAuth,requirePermission}from'../middleware/auth.js';const r=Router();r.use(requireAuth);r.get('/',requirePermission('payroll','view'),list);r.post('/',requirePermission('payroll','create'),create);r.put('/:id',requirePermission('payroll','edit'),update);r.delete('/:id',requirePermission('payroll','delete'),remove);export default r;
