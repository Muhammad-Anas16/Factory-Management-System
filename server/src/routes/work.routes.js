import{Router}from'express';import{list,create,update,complete,remove}from'../controllers/work.controller.js';import{requireAuth,requirePermission}from'../middleware/auth.js';const r=Router();r.use(requireAuth);r.get('/',requirePermission('work-allocation','view'),list);r.post('/',requirePermission('work-allocation','create'),create);r.put('/:id',requirePermission('work-allocation','edit'),update);r.post('/:id/complete',requirePermission('work-completion','edit'),complete);r.delete('/:id',requirePermission('work-allocation','delete'),remove);export default r;
