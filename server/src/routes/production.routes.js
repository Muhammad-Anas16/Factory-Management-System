import {Router} from 'express';import{list,create,update,remove}from'../controllers/production.controller.js';import{requireAuth,requirePermission}from'../middleware/auth.js';const r=Router();r.use(requireAuth);r.get('/',requirePermission('production','view'),list);r.post('/',requirePermission('production','create'),create);r.put('/:id',requirePermission('production','edit'),update);r.delete('/:id',requirePermission('production','delete'),remove);export default r;
