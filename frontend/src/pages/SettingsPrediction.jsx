import PageHeader from '../components/PageHeader';
import { useState } from 'react';
export default function Prediction() { const [value, setValue] = useState(''); return <div className="page"><PageHeader title="Production Prediction" subtitle="Optional forecasting area for future production planning." /><div className="two-column"><section className="panel"><div className="panel-title">Forecast inputs</div><div className="form-stack"><label>Expected next-month pieces<input type="number" value={value} onChange={(e) => setValue(e.target.value)} /></label><label>Note<textarea rows="4" placeholder="Adjust based on recent production history…" /></label><button className="primary-button">Save Forecast</button></div></section><section className="panel"><div className="panel-title">Planning note</div><p className="muted-text">Use previous production data as the baseline, then edit the forecast manually. This is an optional planning module and should not change payroll calculations.</p></section></div></div>; }
