import ResourcePage from './ResourcePage';
import { paymentsApi } from '../api/resources';
export default function Payments() { return <ResourcePage title="Payments" subtitle="Record paid amounts and keep pending balances visible." page="payments" api={paymentsApi} fields={[{ name: 'worker_id', label: 'Worker ID', required: true }, { name: 'amount', label: 'Paid amount', type: 'number', required: true }, { name: 'payment_date', label: 'Payment date', type: 'date', required: true }, { name: 'notes', label: 'Notes', type: 'textarea', full: true }]} columns={[{ key: 'id', label: 'ID' }, { key: 'worker_id', label: 'Worker' }, { key: 'amount', label: 'Amount' }, { key: 'payment_date', label: 'Date' }, { key: 'notes', label: 'Notes' }]} renderCell={(item, key) => key === 'amount' ? `₨ ${Number(item[key] || 0).toLocaleString()}` : undefined} />; }
